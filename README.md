# MacOS Security Homebrew Tap

## How do I install these formulae?

Install the tap via:

```bash
brew tap xhd4/macsec
```

Then you can install individual products via:

```bash
brew install xhd4/macsec/processmonitor
```

